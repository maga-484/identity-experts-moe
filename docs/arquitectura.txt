Entrada (consulta)
      │
      ▼
┌─────────────┐
│   Hashing   │  ← hash = simple_hash(embedding) % 40320
│   8! = 40320│
└─────────────┘
      │
      ▼
┌─────────────┐
│ Tabla Hash  │  ← O(1) lookup
└─────────────┘
      │
      ▼
┌─────────────┐
│   Experto   │  ← MLP tiny (50k parámetros)
│ Identitario │
└─────────────┘
      │
      ▼
   Salida